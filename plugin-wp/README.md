# plugin-wp

WordPress/WooCommerce pairing plugin (PHP, built separately). Implements the
6-digit pairing-code flow from master-plan §9: install plugin → paste code from
the Khodkar panel → keys exchanged securely.

- v1: read (catalog import).
- v1.5: write-back (stock/price updates).

Not started yet — placeholder so the repo structure matches master-plan §4.
